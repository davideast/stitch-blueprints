---
name: "Data Table"
entity: layout
slot: layout
type: structural
tags: [table, data, admin, list-view, sorting, filtering, rows]
expresses: []
prompt: >-
  A full-width table layout: above the table, a toolbar row contains a search input, filter chips or dropdowns, and bulk action buttons; column headers are sticky and contain sort controls; rows are uniform height with consistent cell padding; optional checkbox column on the far left enables row selection; a pagination bar sits below the table with page controls and a per-page count selector.
---

The workhorse of admin and data-heavy applications. Choose this when users need to view, sort, filter, and act on large collections of structured records. The table format enforces alignment that makes scanning and comparison fast. Bulk actions via row selection handle operational workflows efficiently. Pairs with a compact typography spec and a low-distraction aesthetic — the data is the content, the layout is just infrastructure.
